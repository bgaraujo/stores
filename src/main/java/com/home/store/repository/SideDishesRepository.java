package com.home.store.repository;

import com.home.store.entities.meal.SideDishes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SideDishesRepository extends JpaRepository<SideDishes, Long> {
}
